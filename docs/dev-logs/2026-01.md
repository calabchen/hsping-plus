# 🚀 开发日志 | 2026年01月

## 📈 项目概览
- **当前阶段**: PC网页端构建
- **核心目标**: 完成 Laravel 后端与 Vue3 前端的首次通信，跑通 OCR 基础脚本。

---

## 📅 2025-01-05 | 初步构建登录界面
### ✅ 已完成
- [✓] 使用后端 `laravel11` 的 `Fortify`: 一个前端无关的身份验证后端实现。

安装：

```
composer require laravel/fortify
php artisan fortify:install
php artisan migrate
```

在终端中查询安装后的结果：

```
php artisan route:list

  GET|HEAD   / ...................................................................................
  POST       _ignition/execute-solution ignition.executeSolution › Spatie\LaravelIgnition › Execu…
  GET|HEAD   _ignition/health-check ignition.healthCheck › Spatie\LaravelIgnition › HealthCheckCo…
  POST       _ignition/update-config ignition.updateConfig › Spatie\LaravelIgnition › UpdateConfi…
  GET|HEAD   api/user ............................................................................
  GET|HEAD   login ............... login › Laravel\Fortify › AuthenticatedSessionController@create
  POST       login .......... login.store › Laravel\Fortify › AuthenticatedSessionController@store
  POST       logout ............ logout › Laravel\Fortify › AuthenticatedSessionController@destroy
  GET|HEAD   register ............... register › Laravel\Fortify › RegisteredUserController@create
  POST       register .......... register.store › Laravel\Fortify › RegisteredUserController@store
  GET|HEAD   sanctum/csrf-cookie sanctum.csrf-cookie › Laravel\Sanctum › CsrfCookieController@show
  GET|HEAD   up ..................................................................................
  GET|HEAD   user/confirm-password password.confirm › Laravel\Fortify › ConfirmablePasswordContro…
  POST       user/confirm-password password.confirm.store › Laravel\Fortify › ConfirmablePassword…
  GET|HEAD   user/confirmed-password-status password.confirmation › Laravel\Fortify › ConfirmedPa…

  Showing [15] routes
```

在`backend-laravel\config\fortify.php`设置 `'prefix' => 'api',` 采用纯api模式，在终端中查询修改后的结果：

```
php artisan route:list

  GET|HEAD   / ...................................................................................
  POST       _ignition/execute-solution ignition.executeSolution › Spatie\LaravelIgnition › Execu…
  GET|HEAD   _ignition/health-check ignition.healthCheck › Spatie\LaravelIgnition › HealthCheckCo…
  POST       _ignition/update-config ignition.updateConfig › Spatie\LaravelIgnition › UpdateConfi…
  GET|HEAD   api/login ........... login › Laravel\Fortify › AuthenticatedSessionController@create
  POST       api/login ...... login.store › Laravel\Fortify › AuthenticatedSessionController@store
  POST       api/logout ........ logout › Laravel\Fortify › AuthenticatedSessionController@destroy
  GET|HEAD   api/register ........... register › Laravel\Fortify › RegisteredUserController@create
  POST       api/register ...... register.store › Laravel\Fortify › RegisteredUserController@store
  GET|HEAD   api/user ............................................................................
  GET|HEAD   api/user/confirm-password password.confirm › Laravel\Fortify › ConfirmablePasswordCo…
  POST       api/user/confirm-password password.confirm.store › Laravel\Fortify › ConfirmablePass…
  GET|HEAD   api/user/confirmed-password-status password.confirmation › Laravel\Fortify › Confirm…
  GET|HEAD   sanctum/csrf-cookie sanctum.csrf-cookie › Laravel\Sanctum › CsrfCookieController@show
  GET|HEAD   up ..................................................................................

  Showing [15] routes
```

- [✓] 前端 `frontend-pc` 在 `views/auth` 目录中创建 `Register/Login/Dashboard.vue`。


### 🛠 遇到问题
- **问题**: 无

---

## 📅 2025-01-06 | 构建注册界面
### ✅ 已完成

- [✓] 前端 `frontend-pc` 在 `src/views/auth/Register.vue` 文件中使用 `axios` 前端插件。
- [✓] 后端 `backend-laravel` 中启用 `config/cors.php`。
使用 config:publish Artisan 命令发布完整的 cors 配置文件：`php artisan config:publish cors`。然后将本文件中`supports_credentials` 设置为 `true`。

### 🛠 遇到问题
- **问题**: 前后端互相通信进行用户注册时，发送的cookie不一致。
- **解决**: 
  1. 在 `Register.vue` axios 实例上加入 `withCredentials` 和 `withXSRFToken` 选项，并且设置为 `ture`。

---

## 📅 2026-01-07 | 完成登录界面（第1天）
### ✅ 已完成
- [✓] 在 `frontend-pc/src/views/auth/Login.vue` 搭建基础登录表单（邮箱、密码、提交按钮）。
- [✓] 接入 `authStore.login()`，统一调用后端 `POST /api/login`。
- [✓] 提交态处理：增加 `loading`，避免重复点击提交。

### 🛠 遇到问题
- **问题**: 登录失败时只有通用报错，无法快速判断是账号错误还是会话问题。CORS blocked
- **原因**：后端未放行 8001，且带 cookie 时不能用 allowed_origins=['*']
- **解决**:
  1. 先保留用户友好提示 `登录失败，请检查账户和密码。`。
  2. 在后续联调中通过 Network 面板定位具体 HTTP 状态码。
  3. CORS_ALLOWED_ORIGINS 加入 http://localhost:8001,http://127.0.0.1:8001;
SANCTUM_STATEFUL_DOMAINS 加入 localhost:8001,127.0.0.1:8001; config/cors.php 使用明确 origin 列表; 执行 php artisan optimize:clear 并重启服务

---

## 📅 2026-01-08 | 完成登录界面（第2天）
### ✅ 已完成
- [✓] 新增 `frontend-pc/src/services/api.ts`，统一配置 axios：
  - `baseURL = http://localhost:8000`
  - `withCredentials = true`
  - `withXSRFToken = true`
- [✓] 在登录流程前先请求 `GET /sanctum/csrf-cookie`。
- [✓] 完成登录后跳转逻辑：默认进入 `/dashboard`。

### 🛠 遇到问题
- **问题**: 前后端跨端口通信时，偶发出现 cookie 不生效。
- **解决**:
  1. 固化为先拿 CSRF 再发登录请求。
  2. 统一所有请求都通过 `api.ts` 实例发送，避免配置不一致。

---

## 📅 2026-01-09 | 完成登录界面（第3天）
### ✅ 已完成
- [✓] 在 `frontend-pc/src/router/index.ts` 添加登录态守卫。
- [✓] 对 `/dashboard` 增加 `meta.requiresAuth`，未登录时禁止进入。
- [✓] 在 `frontend-pc/src/App.vue` 添加“未登录，请先登录或者注册”弹窗。

### 🛠 遇到问题
- **问题**: 未登录用户点击仪表盘时体验中断，不清楚下一步操作。
- **解决**:
  1. 弹窗内增加 `去登录/去注册` 两个按钮。
  2. 增加转圈动画，提示正在处理状态。

---

## 📅 2026-01-10 | 完成登录界面（第4天）
### ✅ 已完成
- [✓] 为未登录弹窗补充右上角关闭按钮（`x`）。
- [✓] 登录页补充输入提示：邮箱格式、密码长度说明。
- [✓] 登录后在 `Dashboard.vue` 展示当前用户并支持退出登录。

### 🛠 遇到问题
- **问题**: 控制台出现 `401 Unauthorized`，影响联调判断。
- **原因**：跨端口场景下 session cookie SameSite=lax 可能不发送
- **解决**:
  1. 明确区分“初始化探测登录态”的 401 与“登录后仍 401”的问题。
  2. 后端统一开放正确的 CORS 与 Sanctum stateful 域名，修复会话丢失。
  3. .env 设置 SESSION_SAME_SITE=null。php artisan config:clear + 重启后端

补充：应用初始化时未登录去请求 /api/auth/user 返回 401 是正常现象（用于判定未登录）

---

## 📅 2026-01-11 | 完善注册界面（第1天）
### ✅ 已完成
- [✓] 在 `frontend-pc/src/views/auth/Register.vue` 重构为真实注册表单（姓名、邮箱、密码、确认密码）。
- [✓] 去除旧的自动注册测试代码，改为用户手动提交。
- [✓] 接入 `authStore.register()`，调用 `POST /api/register`。

### 🛠 遇到问题
- **问题**: 注册失败时仅显示“注册失败”，定位效率低。
- **解决**:
  1. 捕获后端 `errors` 字段并展示首条验证错误。
  2. 保留兜底提示，避免空白错误信息。

---

## 📅 2026-01-12 | 完善注册界面（第2天）
### ✅ 已完成
- [✓] 增加前端基础校验：密码与确认密码一致性检查。
- [✓] 增加输入约束：
  - 姓名 `maxlength=255`
  - 邮箱 `maxlength=255`
  - 密码 `minlength=8`
- [✓] 增加字段提示文案，降低用户试错成本。

### 🛠 遇到问题
- **问题**: TypeScript 报错：`Type 'undefined' cannot be used as an index type`。
- **解决**:
  1. 读取 `errors` 首键前增加空值保护。
  2. 使用三元表达式提供默认错误文案。

---

## 📅 2026-01-13 | 完善注册界面（第3天）
### ✅ 已完成
- [✓] 与后端规则对齐：恢复默认密码策略 `Password::default()`（至少 8 位）。
- [✓] 在注册页明确提示密码规则：
  - 至少 8 位
  - 不强制大写/小写/特殊字符
- [✓] 登录页同步同样规则提示，避免认知不一致。

### 🛠 遇到问题
- **问题**: 测试中使用 6 位密码触发 422，容易误判为接口异常。
- **原因** ：Fortify 默认密码规则至少 8 位（以及邮箱格式/唯一性等）
- **解决**:
  1. 在前端显式提示密码位数要求。
  2. 在错误展示中直接透传后端验证信息。

---

## 📅 2026-01-14 | 完善注册界面（第4天）
### ✅ 已完成
- [✓] 完成跨域注册联调：
  - `SANCTUM_STATEFUL_DOMAINS` 补充 `localhost:8001`
  - `CORS_ALLOWED_ORIGINS` 补充 `http://localhost:8001`
  - `SESSION_SAME_SITE=null` 支持本地跨端口会话
- [✓] 清理缓存并验证：`php artisan optimize:clear`、`php artisan config:clear`。
- [✓] 测试账号可注册/登录，注册与登录页面流程闭环完成。

### 🛠 遇到问题
- **问题**: 浏览器报 CORS 与 401 交替出现，难以一次定位。
- **解决**:
  1. 先修 CORS（浏览器拦截层）。
  2. 再修会话 SameSite（cookie 发送层）。
  3. 最后校验 Fortify 验证规则（业务层）。

---

## 📅 2026-01-18 | 用户与教师数据结构拆分（第1天）
### ✅ 已完成
- [✓] 调整数据模型：将教师业务字段从 `users` 拆分到 `teachers`。
- [✓] 新增 `backend-laravel/app/Models/Teacher.php`，并在 `User` 模型新增 `teacher(): HasOne` 关系。
- [✓] 补充/整理迁移：
  - `2026_03_06_135126_add_profile_fields_to_users_table.php`
  - `2026_03_08_120000_create_core_prd_tables.php`
  - `2026_03_08_121000_add_subject_to_teachers_table.php`
  - `2026_03_08_123000_drop_mismatched_columns_from_users_table.php`

### 🛠 遇到问题
- **问题**: `users` 与 `teachers` 字段职责混合，后续扩展时容易冲突。
- **解决**:
  1. 明确 `users` 只保留账号字段（name/email/phone/password）。
  2. 将教师画像字段归并到 `teachers`，通过 `user_id` 进行一对一关联。

---

## 📅 2026-01-19 | 个人资料后端 API 闭环（第2天）
### ✅ 已完成
- [✓] 新增 `backend-laravel/app/Http/Controllers/ProfileController.php`。
- [✓] 在 `backend-laravel/routes/api.php` 增加资料相关路由：
  - `GET /api/auth/profile`
  - `PUT /api/auth/profile/user`
  - `PUT /api/auth/profile/teacher`
  - `DELETE /api/auth/account`
- [✓] 统一返回结构：`users` + `teachers`，便于前端表单分别回填。

### 🛠 遇到问题
- **问题**: 修改密码场景需要和普通资料更新共存。
- **解决**:
  1. 仅当提交 `new_password` 时校验 `current_password` 与 `confirmed`。
  2. `Hash::check` 失败时返回明确字段级错误（当前密码不正确）。

---

## 📅 2026-01-20 | Dashboard 路由重构与目录整理（第3天）
### ✅ 已完成
- [✓] 将仪表盘页面迁移到 `frontend-pc/src/views/dashboard/Dashboard.vue`。
- [✓] 新增并接入 `frontend-pc/src/views/dashboard/Profile.vue`。
- [✓] 路由改为嵌套：`/dashboard/profile` 作为 `dashboard` 子路由。
- [✓] `RouterView` 放入 `Dashboard` 右侧 `<main class="main-content">`，个人资料在内容区渲染。

### 🛠 遇到问题
- **问题**: 页面迁移后路由导入路径与激活态判断容易遗漏。
- **解决**:
  1. 在 `router/index.ts` 统一改为 `views/dashboard/*` 导入。
  2. 使用当前路由路径控制侧边栏 active 状态。

---

## 📅 2026-01-21 | Profile 页面联调（第4天）
### ✅ 已完成
- [✓] `Profile.vue` 使用 `DHTMLX Layout + Form` 实现双栏（Users/Teachers）编辑。
- [✓] 实现资料加载与回填：`GET /api/auth/profile`。
- [✓] 实现账号更新与教师更新：
  - `PUT /api/auth/profile/user`
  - `PUT /api/auth/profile/teacher`
- [✓] 完成密码修改可选逻辑与前端一致性校验（新密码/确认密码）。

### 🛠 遇到问题
- **问题**: 保存后成功消息容易被二次加载提示覆盖。
- **解决**:
  1. 移除 `loadProfile()` 中的通用成功提示。
  2. 仅保留“账号信息已更新 / 教师信息已更新”等操作型提示。

---

## 📅 2026-01-22 | Profile 交互与视觉细节优化（第5天）
### ✅ 已完成
- [✓] 顶部消息提示调整到标题右侧，支持单行显示（`white-space: nowrap`）。
- [✓] 调整“修改密码（可选）”与密码字段间距。
- [✓] Users/Teachers 表单标签统一右对齐，输入区宽度与起始位置统一。
- [✓] 修复 Profile 内容区填充问题，使其完整占据 Dashboard 主内容区域。

### 🛠 遇到问题
- **问题**: DHTMLX 默认样式优先级较高，导致 label 对齐不稳定。
- **解决**:
  1. 增加 `:deep(.dhx_form-group__label)` 统一样式与固定宽度。
  2. 对关键对齐属性使用更高优先级覆盖默认样式。

---

## 📅 2026-01-23 | 注册后强制引导完善教师资料（第6天）
### ✅ 已完成
- [✓] `Register.vue` 引入 DHTMLX 弹窗并在注册成功后弹出引导提示。
- [✓] 只保留按钮“立即填写”，点击后直接跳转 `/dashboard/profile`。
- [✓] 弹窗样式采用绿色主题，弱化阻断感但保持强引导。

### 🛠 遇到问题
- **问题**: DHTMLX 弹窗 API 与类型定义存在差异。
- **解决**:
  1. 使用 `dhx.alert()` 实现单按钮强引导场景。
  2. 去除不兼容属性并修正回调类型写法，消除 TS 报错。

---

## 📅 2026-01-24 | 账户注销与级联删除闭环（第7天）
### ✅ 已完成
- [✓] 在 Users Form 增加“注销账户”按钮（与“应用新的账户信息”同一行）。
- [✓] 增加二次确认弹窗，提示不可恢复风险。
- [✓] 前端调用 `DELETE /api/auth/account` 后清空登录态并跳转 `/`。
- [✓] 后端删除用户后通过外键 `cascadeOnDelete` 自动清理关联数据。

### 🛠 遇到问题
- **问题**: 删除后仍可能停留在登录态页面。
- **解决**:
  1. 删除成功后前端立即置空 `authStore.state.user`。
  2. 强制 `router.replace('/')`，避免历史路由回退到受保护页。

---

## 📅 2026-01-25 | 登录稳定性修复与路由守卫优化（第8天）
### ✅ 已完成
- [✓] `Login.vue` 增加防重复提交，减少并发触发 `/api/login` 的概率。
- [✓] 登录前对邮箱执行 `trim().toLowerCase()`，降低格式误差导致的 422。
- [✓] 登录失败优先显示后端返回的具体错误信息（message / field errors）。
- [✓] `router.beforeEach` 增加非鉴权页自动关闭未登录弹窗逻辑，避免状态残留。
- [✓] `authStore.logout()` 改为 `try/finally`，确保本地登录态总能清空。

### 🛠 遇到问题
- **问题**: 登录页偶发“未登录提示弹窗”与 422 报错并存，体验混乱。
- **解决**:
  1. 控制提交节流，避免重复请求导致验证/限流噪音。
  2. 清理弹窗残留状态，确保登录页交互专注。
  3. 将后端真实错误透传到前端，提升问题可定位性。

---

## 📅 2026-01-26 | 班级管理模块初建（第1天）
### ✅ 已完成
- [✓] 创建 `frontend-pc/src/views/dashboard/Class.vue`，新增班级和学生管理界面。
- [✓] 后端实现班级 API 端点：
  - `GET /api/classes` - 获取班级列表
  - `POST /api/classes` - 新建班级
  - `PUT /api/classes/{id}` - 编辑班级
  - `DELETE /api/classes/{id}` - 删除班级
  - `GET /api/classes/{classId}/students` - 获取班级学生列表
- [✓] 前端使用 DHTMLX Layout 实现左右两栏布局（班级 Grid + 学生 DataView）。
- [✓] 从班级模型数据 `enrollment_year + class_num` 构建完整班级名称（如"2025级01班"）。

### 🛠 遇到问题
- **问题**: 班级名称构建逻辑需要在多处重复。
- **解决**:
  1. 统一在 `normalize()` 函数中生成 `displayName` 字段。
  2. Grid 列表与后续导出文件均使用同一逻辑，保证一致性。

---

## 📅 2026-01-27 | 班级 UI 与学生展示（第2天）
### ✅ 已完成
- [✓] 班级 Grid 列：班级名称、学生人数、入学年份、毕业年份、状态（已毕业/未毕业）。
- [✓] 班级 Toolbar 按钮：新建班级、编辑班级、删除班级。
- [✓] 学生 DataView 使用卡片式模板，展示：学号、姓名、性别、年龄、头像。
- [✓] 学生 Toolbar 按钮：下载导入模板、手动添加、批量上传、导出Excel、编辑学生、删除学生、刷新信息。
- [✓] 修复加载覆盖层位置：限制在右侧学生区域（`top: 56px, left: 40%, right: 0`），不覆盖班级表。

### 🛠 遇到问题
- **问题**: 初始加载学生列表时覆盖层覆盖整个界面，用户体验差。
- **解决**:
  1. 使用 CSS 定位 `student-loading-overlay--student-content` 限制覆盖范围。
  2. 确保工具栏（`height: 56px`）不被覆盖，便于用户操作。

---

## 📅 2026-01-28 | Excel 导入导出功能实现（第3天）
### ✅ 已完成
- [✓] 安装 `xlsx` 库（v0.18.5）用于 Excel 读写。
- [✓] 实现导出功能：
  - `downloadStudentTemplate()` - 导出标准导入模板（含示例行"王小明"）
  - `downloadStudentsExcel()` - 导出当前班级学生数据
  - 模板包含 6 列：学号、姓、名、性别、年龄、头像URL
- [✓] 实现导入功能：
  - `parseXlsxToStudents()` - 解析 Excel 文件
  - `parseCsvToStudents()` - 解析 CSV 文件
  - `normalizeGender()` - 性别值规范化（接收男/女/male/female/m/f/1/0，统一输出男/女）
- [✓] 调用 `/api/students/bulk` 批量创建学生。

### 🛠 遇到问题
- **问题**: Excel 模板用户无法直接输入大量行数据，效率低。
- **解决**:
  1. 在模板导出时预生成 200 行空白行，用户可直接填充。
  2. 第二行为示例行（"004", "王", "小明"），方便参考。

---

## 📅 2026-01-29 | 导出学号格式与列结构优化（第4天）
### ✅ 已完成
- [✓] 修复导出学号丢失前导零问题（如"004"被自动转换为"4"）：
  - 使用 XLSX `aoa_to_sheet()` 替代 `json_to_sheet()`
  - 将学号列单元格类型设置为文本（`t: 's'`）
  - 设置格式代码为 `@`（文本格式）
- [✓] 调整导入模板解析，使用 `sheet_to_json(..., raw: false)` 保留格式化文本值。
- [✓] 调整导出列顺序与命名：
  - 学号导出 Excel：学号、姓、名、全名（姓+名拼接）、性别、年龄
  - 移除头像URL列（导出数据不含头像）
  - 导入模板保留完整 6 列（含头像URL）供用户参考

### 🛠 遇到问题
- **问题**: "全名"列位置与用户期望不符。
- **解决**:
  1. 将全名列从最后移到姓名之后（学号、姓、名、全名、性别、年龄）
  2. 前端 DataView 展示时继续使用姓+名拼接逻辑，保持一致。

---

## 📅 2026-01-30 | 班级名称显示与文件名完整性修复（第5天）
### ✅ 已完成
- [✓] 修复后端 `ClassController@students()` 返回字段：
  - 原仅返回 `class_id + class_num`
  - 新增 `enrollment_year` 字段，支持前端完整构建班级名称
- [✓] 改进前端 `loadStudentsByClass()` 逻辑：
  - 从 API 返回的 `class` 对象提取 `enrollment_year`
  - 使用 `.trim()` 和 `!== '0'` 判断避免空值与"0"被视为 false
  - 同步更新工具栏显示（`studentToolbar.data.update('className', ...)`）
- [✓] 修复导出文件名：从"01班学生信息.xlsx"改为"2025级01班学生信息.xlsx"。
- [✓] 消除 `cellClick` 事件中的竞态条件：不再预设 `selectedClassName`，让 `loadStudentsByClass()` 统一从 API 获取最新数据。

### 🛠 遇到问题
- **问题**: 文件名显示为"01班"而非"2025级01班"，说明 `selectedClassName.value` 未正确赋值。
- **原因**: 
  1. API 未返回 `enrollment_year` 字段
  2. 竞态：`cellClick` 预设的 `displayName` 与异步 `loadStudentsByClass()` 修改冲突
- **解决**:
  1. 后端补充 `enrollment_year` 返回值
  2. 前端移除预设赋值，完全由 `loadStudentsByClass()` 统一处理

---

## 📅 2026-01-31 | 重复学号与错误处理优化（第6天）
### ✅ 已完成
- [✓] 优化重复学号处理逻辑（`uploadStudents` 函数）：
  - 上传前本地过滤，移除与当前班级已有学生重复的学号
  - 不报错，而是在成功消息中提示"跳过重复学号 X 条"
  - 若服务端仍返回重复错误（并发操作），自动重试过滤
- [✓] 调整导入错误提示：
  - 全部重复：`所有学号都已存在，未添加任何学生`
  - 部分重复：`批量导入成功（5人），跳过重复学号3条`
- [✓] 完整 Excel 导出流程闭环：模板下载 → 用户填充 → 批量导入 → 自动刷新列表。

### 🛠 遇到问题
- **问题**: 用户上传包含重复学号的 Excel 时直接报错，无法确认哪些有效数据已被保存。
- **解决**:
  1. 前端主动过滤本地 `studentsCache` 中的重复学号，降低冲突概率。
  2. 即使服务端返回重复错误，也自动移除重复记录后重试。
  3. 提示信息改为正面导向（成功+跳过数量），而非单纯错误。

---